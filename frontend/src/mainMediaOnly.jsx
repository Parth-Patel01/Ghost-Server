import React from 'react'
import ReactDOM from 'react-dom/client'
import AppMediaOnly from './AppMediaOnly.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AppMediaOnly />
  </React.StrictMode>,
) 